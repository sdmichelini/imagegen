# Architecture

This project follows the Go project layout guidance from:
https://github.com/golang-standards/project-layout

- `cmd/imagegen` contains the CLI entrypoint.
- Root-level files contain project metadata and build configuration.
